"""
solar_platform.core
===================

Lightweight shared layer — stdlib + requests only, no heavy platform deps.

Provides:
  core.clients.*       — API auth helpers and base URLs for each platform
  core.site_registry   — canonical site ID / name lookup (single source of truth)
"""
