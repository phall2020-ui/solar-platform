# detect_curtailment_interval() was removed — the stub required a
# controller_active boolean with no defined data source and was unused.
# Curtailment tests live in tests/unit/test_curtailment_engine.py.
