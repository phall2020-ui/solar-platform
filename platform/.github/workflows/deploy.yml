name: Deploy

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and tag image (placeholder)
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          docker build -t unified-app:${VERSION} .
          docker tag unified-app:${VERSION} unified-app:latest

      - name: Push placeholder
        run: echo "Configure registry login and push steps here"
